    input_datetime_state:
        component: meya.input_datetime
        properties:
            text: "Your question here"
            output: <YOUR_KEY>
            scope: <SCOPE> # e.g. flow|user|bot
            timezone: <TIMEZONE> # e.g. UTC
            timezone_scope: <SCOPE> # e.g. flow|user|bot
            parser: <PARSER> # e.g. V1.0|V2.0
            require_match: <BOOLEAN>