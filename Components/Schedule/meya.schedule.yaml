    start_schedule_state:
        flow: meya.schedule
        data:
            tag: <YOUR_TAG>
            flow: <YOUR_FLOW>
            period: <PERIOD> # seconds|minutes|hours|weeks|months|years
            interval: <INTERVAL> # e.g. 2
            max_count: <MAX_COUNT> # e.g. 30